// cors
// http://subdomain.domain.com:4567/path/to/file

// const exampleOne = function(one) {

// };

// exampleOne()
//   .then((d) => {
//     exampleTwo(d);
//   })
    // .catch(err => console.log('Fetching error:', err));

// var Promise = require('bluebird');

// take old code and turn it into a promise without using .promisify

// var myPromise = new Promise();
// var myFunc = function myFunc(){
//   // more code up here
//   return new Promise(resolve, reject) {
//     if (err) { reject(err) }
//     resolve('data');
//   }
// };
// var = sendEmailofAllpasswordstome,thehacker()
// eval();

// Give you an array
// Output: Merge sorted array

// const mergeSort = function(arr) {

//   const merge = function(arr1, arr2) {
//     let results = [];

//     while (arr1.length && arr2.length) {
//       results.push(arr1[0] < arr2[0] ? arr1.shift() : arr2.shift())
//     }
//     return results.concat(arr1.length > 0 ? arr1 : arr2)
//   };

//   if (arr.length < 2) {
//     return arr;
//   }

//   let mid = arr.length / 2;
//   let left = arr.slice(0, mid);
//   let right = arr.slice(mid);

//   return merge(mergeSort(left), mergeSort(right));
// };


// console.log(JSON.stringify(mergeSort([2,1,4,6,5,3])) === JSON.stringify([1,2,3,4,5,6]));


// Give you an array of stock prices:
// get hourly prices
// 9, 10, 11, 12, 1  2
// [5, 6, 4,   8, 9, 2];

// [10, 9, 7, 2, 1, -20]; // => -1

// give me the maximum profit you could make in that day
// ex.: 5

// 1
// 4
// 5


const findBiggestDiff = function(arr) {


}
